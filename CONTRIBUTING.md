# Contributing to Scale-R AI Website

Thank you for your interest in contributing to the Scale-R AI website! This document provides guidelines and instructions for contributing.

## Getting Started

1. Fork the repository
2. Clone your fork: `git clone https://github.com/YOUR-USERNAME/scale-r.git`
3. Create a new branch: `git checkout -b feature/your-feature-name`
4. Make your changes
5. Test your changes thoroughly
6. Commit your changes: `git commit -m "Add: your feature description"`
7. Push to your fork: `git push origin feature/your-feature-name`
8. Open a Pull Request

## Code Style

### TypeScript/React

- Use functional components with hooks
- Use TypeScript for type safety
- Follow the existing code structure
- Use meaningful variable and function names
- Add comments for complex logic

### CSS/Styling

- Use Tailwind CSS utility classes
- Follow the existing design system
- Maintain responsive design for all screen sizes
- Test on mobile, tablet, and desktop

### Accessibility

- Always include ARIA labels for interactive elements
- Ensure keyboard navigation works
- Test with screen readers when possible
- Maintain proper heading hierarchy

## Commit Messages

Use clear, descriptive commit messages:

- `Add:` for new features
- `Fix:` for bug fixes
- `Update:` for changes to existing features
- `Remove:` for removing code/files
- `Docs:` for documentation changes

Examples:

- `Add: newsletter signup form`
- `Fix: mobile menu not closing on route change`
- `Update: improve form validation`

## Pull Request Guidelines

1. Describe what your PR does
2. Reference any related issues
3. Include screenshots for UI changes
4. Ensure all tests pass
5. Keep PRs focused on a single feature/fix

## Testing

Before submitting a PR:

- [ ] Test on Chrome, Firefox, and Safari
- [ ] Test on mobile devices
- [ ] Check for console errors
- [ ] Verify responsive design
- [ ] Test accessibility features

## Questions?

Contact the team at scaleraigh@gmail.com

Thank you for contributing! 🚀
